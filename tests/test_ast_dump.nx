// "nx ast файл.nx" (AstJsonDumper.cs) - перевіряється ОКРЕМИМ блоком у
// run_all.sh (запускає CLI як зовнішній процес, бо цей режим - властивість
// самого CLI, не функція мови, викликати "з середини" .nx-скрипта нема як).
// Цей файл - лише фікстура з відомими структурами (return не останнім,
// порожній catch), яку той блок згодовує "nx ast" і перевіряє JSON.

func f() {
    return 1
    print("мертвий код")
}

func g() {
    try {
        h()
    } catch (e) {
    }
}
